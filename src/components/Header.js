import React from 'react';

const Header = () => {
    return (
        // Header
        <header>
            <h1>Hangman</h1>
            <p>Can you find the hidden word? Enter a letter and let's find out!</p>
        </header>
        // End Header
    );
};

export default Header;